# AI Content System — Offer Page

Static landing page for the **AI Content System Install** offer ($497 / $997 / $1,497).

## Deploy

This folder is a static site (`index.html` only). Point Railway (or any static host) at this repo’s root; no build step required.

Stripe payment links are placeholders (`#stripe-link-*`) until wired.

## Stack

- Plain HTML + CSS
- No backend, no npm, no framework
